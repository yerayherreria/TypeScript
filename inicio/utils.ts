export function duplicarNumero(numero: number): number {
    return numero * 2;
}

const PI = 3.1416;

export function areaCirculo(radio: number): number {
    return PI * radio * radio;
}

export function longitudCirculo(radio: number): number {
    return PI * radio * 2;
}